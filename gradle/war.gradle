apply plugin: "war"

bootWar {
    mainClassName = "com.bitsignite.myapp.JhipsterSampleApplicationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
